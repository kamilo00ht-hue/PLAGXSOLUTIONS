# Web SaaS App (existing)

La aplicación SaaS principal permanece en la raíz del repositorio (`src/`, `package.json`, `next.config.ts`).

Este directorio se incluye para cumplir la estructura multiplataforma requerida.
